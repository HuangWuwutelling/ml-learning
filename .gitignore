# 公众号文章（不上传 GitHub，本地有独立 git 仓库）
articles/

# 项目本地元数据（plan.md 等），不上传 GitHub，本地有独立 git 仓库
_local/

# 辅助脚本（封面图、notebook 生成等，与算法本身无关）
scripts/

# 测试脚本（本地验证用，不上传 GitHub）
tests/

# Python
__pycache__/
**/__pycache__/
*.pyc
*.pyo
.env
.venv/

# Jupyter
.ipynb_checkpoints/
notebooks/*.png
notebooks/_temp_*

# Logs
*.log

# 自动下载的数据集
data/chinook.db
data/chinook_cache/

# 项目调试 / 临时文件
projects/env_agent/data/
projects/ml_playground/_debug.py
projects/ml_playground/_test_*

# 未发布项目（gb15618-miniapp 不上传 GitHub；gb15618 源码已迁移到独立仓库，目录中只保留跳转 README）
projects/gb15618-miniapp/

# wali_cmd 是独立 git 仓库（本地管理，不上传），但保留一个 README 跳转到本地路径
# 整个目录不跟踪，但 README 例外（用于远端显示项目入口）
projects/wali_cmd/
!projects/wali_cmd/README.md

# 项目 systemd 服务文件（本地部署用，不上传 GitHub）
projects/ml_playground/ml-playground.service

# OS
Thumbs.db
.DS_Store
CLAUDE.md

# Test cache
.pytest_cache/

# Claude Code worktrees & auto-memory
.claude/

# MCP config (local machine specific)
.mcp.json
